import Layout from "@/components/layout";
import PrivacySettings from "@/components/organisms/privacy-settings";

const Page = () => {
  return (
    <Layout pageTitle="Privacy Settings">
      <PrivacySettings />
    </Layout>
  );
};

export default Page;
