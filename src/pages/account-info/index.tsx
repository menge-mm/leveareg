import Layout from "@/components/layout";
import AccountInfo from "@/components/organisms/account-info";

const Page = () => {
  return (
    <Layout pageTitle="Account Information">
      <AccountInfo />
    </Layout>
  );
};

export default Page;
