import Layout from "@/components/layout";
import SettingsUI from "@/components/organisms/settings";
import React from "react";

const Settings = () => {
  return (
    <Layout pageTitle="New Settings">
      <SettingsUI />
    </Layout>
  );
};

export default Settings;
